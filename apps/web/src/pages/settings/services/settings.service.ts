import {
  getSettings,
  updateProfile,
  updatePreferences,
  changePassword,
} from "../api/settings.api";

export const settingsService = {
  getSettings,

  updateProfile,

  updatePreferences,

  changePassword,
};
